# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "typerelay"

[[actions]]
name = "Changelog"
icon = "run"
command = "codex resume --last --include-non-interactive --no-alt-screen '$managani-changelog Finalize the Typerelay changelog for the whole completed change in this task. Select only typerelay from .codex/managani-changelog.json. Create an unpublished draft only; preserve the existing per-task duplicate protection.'"

[setup]
script = ""

[[actions]]
name = "compose up"
icon = "run"
command = '''if test (hostname) = "NadaMini"
	dbh-run compose -- up -d
else
	docker compose up -d
end'''

[[actions]]
name = "compose restart"
icon = "run"
command = '''if test (hostname) = "NadaMini"
	dbh-run compose -- restart
else
	docker compose restart
end'''

[[actions]]
name = "compose rebuild"
icon = "run"
command = '''if test (hostname) = "NadaMini"
	dbh-run compose -- up -d --build --force-recreate
else
	docker compose up -d --build --force-recreate
end'''

[[actions]]
name = "compose stop"
icon = "run"
command = '''if test (hostname) = "NadaMini"
	dbh-run compose -- stop
else
	docker compose stop
end'''

[[actions]]
name = "compose status"
icon = "run"
command = '''if test (hostname) = "NadaMini"
	dbh-run status
else
	docker compose ps
end'''

[[actions]]
name = "compose logs"
icon = "run"
command = '''if test (hostname) = "NadaMini"
	dbh-run logs -- --follow
else
	docker compose logs --tail=200 -f
end'''

[[actions]]
name = "compose tests"
icon = "run"
command = '''if test (hostname) = "NadaMini"
	dbh-run test
else
	docker compose exec app pnpm test
end'''

[[actions]]
name = "release"
icon = "run"
command = "./release.sh"
