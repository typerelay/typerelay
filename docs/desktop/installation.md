# Installation

Use the repository installer for Omarchy and the matching panel package. The Windows installer includes the panel plus **TypeRelay TUI** in the Start menu; both use the same `%LOCALAPPDATA%\TypeRelay` database and sync credentials. Native macOS and Windows packages remain beta and may be unsigned. Preserve local settings and credentials when upgrading. See the repository delivery checklist for build details.
