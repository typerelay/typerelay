# Terminal editor

Run `typerelay-tui` to edit local libraries. Ctrl+S saves. F2 moves between fields and leaves the code editor; Tab inserts indentation inside code. F9 switches Text/Code. Space selects entries; Ctrl+A selects visible matches, Ctrl+D clears selection. F8 moves entries. Trash retains recovery for 30 days.
