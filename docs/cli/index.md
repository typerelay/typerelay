# Command line

The existing `typerelay` command controls the expansion client and local installation. Run `typerelay --help` for the installed version’s commands. No separate public-API CLI is introduced by the API/MCP release.
