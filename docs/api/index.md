# Overview

The public API uses `/api/v3`. Existing web routes and desktop sync protocol 4 remain separate. See [Authentication](./authentication), [Workflows](./workflows), and generated OpenAPI operations in the sidebar. Download the [OpenAPI definition](../openapi.json).
