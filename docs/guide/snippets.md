# Text and code snippets

Entries have optional titles and abbreviations. Store bare abbreviations; the expansion prefix stays in local settings. Entries without abbreviations remain searchable and copyable. Code snippets preserve whitespace and language metadata. Dynamic commands are literal text and are never executed.
