# Libraries and sharing

Create a private library or import an export. Sharing assigns members or groups. Assigned editors can edit snippets when enabled; only the creator and authorized account admins manage sharing. Account admins cannot inspect another member’s private libraries.
