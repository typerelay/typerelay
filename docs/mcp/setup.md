# Setup

Configure the exact MCP resource URL and use OAuth sign-in, or a personal access token. For development the endpoint is `http://localhost:3041/mcp`. Production requires HTTPS. Account permissions and token scopes apply to every tool call. Revoke integrations from Settings → Access tokens.
