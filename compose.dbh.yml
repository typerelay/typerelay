services:
  app:
    ports: !override []
    networks:
      default:
      dbh_proxy:
        aliases:
          - "${DBH_PROFILE}-typerelay-app"

  mcp:
    ports: !override []
    networks:
      default:
      dbh_proxy:
        aliases:
          - "${DBH_PROFILE}-typerelay-mcp"

networks:
  dbh_proxy:
    name: "${DBH_PROXY_NETWORK}"
    external: true
