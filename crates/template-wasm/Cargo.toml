[package]
name = "typerelay-template-wasm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
typerelay-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
